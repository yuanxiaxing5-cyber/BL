模块说明
                        模块以实现自动更新包名(需要zygisk模块)
                        配置隐藏应用只支持oss版本162及一下
                        去除更新密钥–因为我会同步更新模块内的密钥所以不需要
                        天机开机USB自动打开的问题也解决了
                        ----------------------------------------------
                        自动更新模块需要zygisk是因为普通这种类型模块需要无线循环添加包名,使用zygisk后就可以做到安装软件才会触发更新这样功耗几乎没有                   
